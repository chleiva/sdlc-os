# gpu-node-pool/azure — Phase 1.5 stub

Not a working Azure GPU node pool. Satisfies the module contract
(identical `variables.tf` / `outputs.tf` to `gpu-node-pool/aws`) only. See
`main.tf` for what the real implementation needs. Build and validate
against a real Azure subscription with GPU quota as its own scoped piece
of work.
